{
  "name": "entropy-garden",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "preview": "bash scripts/dev/preview.sh"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
